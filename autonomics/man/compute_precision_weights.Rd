% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_omics.R
\name{compute_precision_weights}
\alias{compute_precision_weights}
\title{Compute voom precision weights}
\usage{
compute_precision_weights(object, design = object \%>\%
  create_voom_design(), plot = TRUE, verbose = FALSE)
}
\arguments{
\item{object}{SummarizedExperiment: exprs(.) with log2cpm, counts(.) with raw counts.}

\item{design}{design matrix}

\item{plot}{logical}

\item{verbose}{logical(1)}

\item{...}{passed to limma::voom() -> limma::lmFit()}
}
\description{
Compute voom precision weights
}
\examples{
require(magrittr)
if (require(autonomics.data)){
   object <- 'extdata/stemdiff/rnaseq/gene_counts.txt'  \%>\%
              system.file(package = 'autonomics.data')  \%>\%
              read_rnaseq()
   autonomics.import::counts(object) <- autonomics.import::exprs(object)
   object \%>\% compute_precision_weights() \%>\% extract(1:3, 1:3)
}
}
