% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_omics.R
\name{prep_phosphosites}
\alias{prep_phosphosites}
\title{Prepare phosphosites}
\usage{
prep_phosphosites(object, min_localization_prob = 0.75,
  proteingroups_file = paste0(dirname(file), "/proteinGroups.txt"),
  verbose = FALSE)
}
\arguments{
\item{min_localization_prob}{numeric(1)}

\item{proteingroups_file}{character(1)}

\item{verbose}{logical(1)}

\item{file}{character(1)}

\item{quantity}{NULL or value in names(maxquant_patterns)}

\item{fvars}{character(n)}

\item{min_localization_prob}{numeric(1)}

\item{proteingroups_file}{character(1)}

\item{verbose}{logical(1)}
}
\description{
Prepare phosphosites
}
\examples{
require(magrittr)
if (require(autonomics.data)){
   file <- 'extdata/stemdiff/maxquant/phospho (STY)Sites.txt' \%>\%
            system.file(package = 'autonomics.data')
   file \%>\% read_phosphosites()
   file \%>\% read_phosphosites() \%>\% prep_phosphosites()
}
require(magrittr)
if (require(autonomics.data)){
   file <- 'extdata/stemdiff/maxquant/phospho (STY)Sites.txt' \%>\%
            system.file(package = 'autonomics.data')
   file \%>\% read_phosphosites()
   file \%>\% read_phosphosites() \%>\% prep_phosphosites()
}
}
