% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_omics.R
\name{libsizes}
\alias{libsizes}
\alias{counts_to_cpm}
\alias{cpm_to_counts}
\title{Convert between counts and cpm (counts per million scaled reads)}
\usage{
libsizes(counts)

counts_to_cpm(counts, lib.size = libsizes(counts), verbose = FALSE)

cpm_to_counts(cpm, lib.size)
}
\arguments{
\item{counts}{count matrix}

\item{lib.size}{scaled library sizes (vector)}

\item{verbose}{logical(1)}

\item{cpm}{cpm matrix (counts per million scaled reads)}
}
\value{
cpm matrix
}
\description{
Convert between counts and cpm (counts per million scaled reads)
}
\examples{
require(magrittr)
if (require(subramanian.2016)){
   counts <- 'extdata/rnaseq/gene_counts.txt' \%>\%
              system.file(package = 'subramanian.2016') \%>\%
              read_rnaseq_asis()     \%>\%
              autonomics.import::exprs()
   lib.size <- counts \%>\% libsizes()
   cpm      <- counts \%>\% counts_to_cpm(lib.size)
   counts2  <- cpm    \%>\% cpm_to_counts(lib.size)
   sum(counts-counts2)
}
}
